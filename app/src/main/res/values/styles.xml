<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo">
        <!-- Customize your theme here. -->
        <item name="colorPrimary" type="attr">@color/colorPrimary</item>
        <item name="colorAccent" type="attr">@color/colorAccent</item>
    </style>

</resources>
